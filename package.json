{
  "name": "hfut-forum",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp simplified-forum.html dist/index.html && [ -d assets ] && cp -r assets dist/ || true",
    "start": "echo \"请使用静态服务器打开dist/index.html\"",
    "deploy": "npm run build && tcb framework deploy"
  },
  "dependencies": {},
  "devDependencies": {}
}
